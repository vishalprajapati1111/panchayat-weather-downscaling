# requirements.txt — exact versions verified on Python 3.12.10 / Windows 11
# GDAL 3.12.4, PROJ 9.5.1 (bundled inside rasterio wheel)
#
# Install:
#   pip install -r requirements.txt
#
# Note: On Windows, pip installs binary wheels containing GDAL and PROJ automatically.
# On Linux/macOS without system GDAL, conda is recommended (see environment.yml).

rasterio==1.5.1
xarray==2026.7.0
zarr==2.18.7
earthengine-api==1.7.41
numpy==2.3.5
pandas==3.0.3
pyproj==3.7.2
scipy==1.18.0
matplotlib==3.11.1
shapely==2.1.2
tqdm==4.68.3
fsspec==2026.7.0
gcsfs==2026.8.0
requests==2.34.2
